# .github/workflows/hello.yml
name: Test Pipeline

on:
  push:
    branches: [ main ]

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps: 
      - name: Print a message
        run: echo "Test Pipeline"

      - name: Show the date
        run: date
